describe('결제 후 구독생성하는 함수 테스트', () => {
  it('동일 결제 id로 생성된 구독이 있으면 에러를 반환', () => {});

  it('존재하지 않는 상품일 경우 에러를 반환', () => {});

  it('구독 객체 생성 중 오류가 발생되면 에러를 반환', () => {});

  it('구독이 성공적으로 생성되어 ok 200을 반환', () => {});
});
